@model IEnumerable<EMSApp.Models.DIVISION_INFO>

@{
    ViewBag.Title = "Divisions";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container-fluid" style="padding-top:15px">
    <div class="row">
        <div class="col-md-3">
            <p class="btn btn-block btn-outline-success btn-md">
                @Html.ActionLink("Create New", "Create")
            </p>
        </div>
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Divisions</h4>
                    <div class="table-responsive">
                        <table class="table color-bordered-table info-bordered-table">
                            <thead>
                                <tr>
                                    <th></th>
                                    <th>
                                        Department
                                    </th>
                                    <th>
                                        Division
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null)
                                {
                                    foreach (var item in Model)
                                    {
                                <tr>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.DIV_ID })
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DEPARTMENT_INFO.DEPT_TITLE)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DIV_TITLE)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Delete", "Delete", new { id = item.DIV_ID })
                                    </td>
                                </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
