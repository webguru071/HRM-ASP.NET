@model IEnumerable<EMSApp.Models.TRANSACTION_ITEM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TRNS_TITLE)
        </th>        
        <th>Action</th>
    </tr>

    @if (Model != null)
    {
        foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TRNS_TITLE)
                </td>
                <td>
                    @Html.ActionLink("View", "Edit", new { id = item.TRNS_ID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.TRNS_ID })
                </td>
            </tr>
        }
    }
</table>
