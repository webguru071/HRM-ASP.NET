@model EMSApp.Models.ATTENDANCE_DETAILS

@{
    ViewBag.Title = "Daily Attendance";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal" style="padding-top:15px">       
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Daily Attendance </h4>
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <form class="form-material m-t-40">
                    <div class="form-group">
                        Status
                        <div class="col-md-10">
                            @Html.DropDownList("STATUS", new SelectListItem[]
                                                           {
                                                                new SelectListItem { Text = "Select One", Value="" },
                                                               new SelectListItem { Text = "Check In", Value=EMSApp.Helper.ConstantValue.AttendanceCheckIn },
                                                               new SelectListItem { Text = "Check Out", Value=EMSApp.Helper.ConstantValue.AttendanceCheckOut },},
                                                               htmlAttributes: new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        Password
                        <div class="col-md-10">
                            <input id="pass" name="pass" type="password" class="form-control" />
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary pull-right">Enter</button>
                </form>
            </div>
        </div>
    </div>
}


